# Create a suitable environment:
# mamba env create -f environment.yml 
# or 
# conda env create -f environment.yml

name : smmregrid
channels:
  - conda-forge
dependencies:
  - python>=3.7,<3.11
  - numpy
  - netcdf4 
  - dask
  - xarray
  - cfgrib
  - xesmf
  - cfunits
  - numba
  - cdo
  - python-cdo
  - pytest
  - ipykernel
  - pip
  - pip:
    - sparse
