# Create a suitable environment:
#   conda env create -f environment.yml

name : smmregrid
channels:
  - conda-forge
dependencies:
  - python>=3.7,<3.11
  - numpy
  - netcdf4 
  - dask
  - xarray
  - cfgrib
  - xesmf
  - cfunits
  - cdo
  - python-cdo
  - pytest
  - ipykernel
  - pip:
    - sparse==0.13.0
