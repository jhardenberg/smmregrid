# Create a suitable environment:
#   conda env create -f environment.yml

name : smmregrid
channels:
  - conda-forge
dependencies:
  - python>=3.8,<3.11
  - numpy
  - netcdf4 
  - dask
  - xarray
  - xesmf
  - sparse
  - cfunits
  - cdo
  - python-cdo
